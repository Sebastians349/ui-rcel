import React from 'react';
import { Heading } from '@chakra-ui/react';

const MainBoxTitle = () => {
  return (
    <Heading
      fontSize="xs"
      fontWeight="medium"
      textTransform="uppercase"
      color="blue"
    >
      Seleccione la empresa a representar
    </Heading>
  );
};

export default MainBoxTitle;
